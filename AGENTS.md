CLI for querying the ECMAScript specification. Designed for LLM agent consumption.
All output is deterministic, structured JSON or markdown to stdout. No ANSI codes.

Build: `make` (uses goreleaser) — Install: `go install .`
Run tests: `go test ./...` — Lint: `golangci-lint run`
